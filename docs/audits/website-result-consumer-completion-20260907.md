# Result consumer follow-up completion matrix

This is the local implementation closeout for ADP-007 / ADP-004 supporting ADP-009D public-scene day 28. It supersedes the unresolved implementation/coverage entries in the first-pass [audit](website-result-consumer-20260907.md). No merge, push, deployment, provider allocation, policy/model call, profile publication, or live email send is part of this closeout.

Base main was re-fetched and remained `5b07fe48e93cabf2a758b7740aab3d6594cbdd10`. The first local repair is `29bf82389a751c94e401959c47105a442e7fc9a9`. Follow-up fixes remain on `codex/website-result-audit-20260907-24c1` in `/private/tmp/website-result-audit-20260907-24c1`. Exported `checks.json` and `INTEGRATION.json` identify the exact final tested commit and working-tree state.

## Requirement-by-requirement evidence

| Requirement / finding | Implemented behavior | Verification |
|---|---|---|
| Paired headline versus marginal cohort | Direction, delta, and sign test use the same unique mutually scorable cells/seeds. Overall rates retain their own denominators and exclusions. | Independent ten-cell probe before/after; disjoint/unequal cohorts, no pairs, ties, one pair, unknown/null/missing scores, order reversal, and duplicate regressions. |
| Preserve failed and ambiguous episodes | Explicit interpretability and boolean score are required for arithmetic. Duplicates remain visible even under filters; filtering selects cells without hiding the other candidate. | Helper and portal tests; fixed failure counts are not selectively removed. |
| Verified score corrections | Source projection/delivery bindings must match. Corrections cannot change execution completion counts or erase uninterpretable harness failures. | Correction regression preserves 12 reported completions while independently verifying nine corrected paired ties; stale correction rejected. |
| Corrected criteria missing from review | A common, cryptographically validated correction contract is projected separately. Registry defaults never become team confirmation or original request authorization. | Server contract tests, result-panel tests, and the actual retained correction's contract readback. |
| Lifecycle, counts, and terminal authority | Learned and control totals stay separate. Whole status-packet digests identify replays. Older/same-time conflicting updates, ownership drift, and late pre-provider downgrades are refused. Terminal status remains distinct from retained execution phase. | Signed local route tests; replay/conflict tests; terminal-phase tests; prior late-closeout tests retained. |
| Expired/revoked accepted intents | Existing read-only late-closeout path preserved. Unissued intents can stop locally; accepted attempts can report closeout without resubmission. | Existing scene-intake test cases include expiry, revocation, bounded polling, and wrong-owner refusal (25 tests in that file). |
| Transient reads and identity changes | Header/read deadlines, exponential backoff, Retry-After, stale-data notices, owner/tenant/run isolation, unmount cancellation, and terminal stopping. Result-query errors preserve prior data only when access is still permitted. | Fake timers, hook/component regressions, and a real progress-page browser flow. |
| CSRF recovery | Token reads reuse a valid existing browser cookie, preventing cross-tab invalidation. A result POST may refresh once only after the explicit pre-handler CSRF rejection. Other permission refusals stop. | Real middleware tests retain missing/mismatched-token rejection; client and browser stale-token recovery tests. |
| Stream integrity | Full bodies are hashed incrementally, including chunked bodies. The final byte is withheld until length/hash checks pass. Range bounds/length/source digest are validated, with partial checksum scope explicitly distinguished. | Controlled native streams plus real Website routes against a signed local origin; corrupted/truncated bodies fail closed. |
| Media refusal and stalled connections | Safe 401/403/404/429/5xx/416 handling; Retry-After and Content-Range retained where meaningful; disconnect propagation, header/idle deadlines, no secret-bearing redirect following. | Stream/probe fault regressions and local HTTP-chain tests. |
| Browser media and attachment recovery | Video readiness requires a decoded frame. Failed/expired access gets a fresh ticket; owner changes cancel pending reads. JSON receipts enforce byte limits/digest before parsing. Download failures remain actionable. | Component lifecycle tests and real mobile browser playback with an actual fixture WebM. |
| Compacted/full evidence inventory | Inline omissions are explicit; the verified full manifest can be loaded on request and its inventory paginated. Descriptor presence is never presented as proof all bytes are available. | Full-manifest unit/browser recovery tests; actual record expands from 516 inline descriptors to 5,056 total descriptors. |
| Native manifest / absent frame | Native-format fixture is validated using Pipeline's actual reader with file verification. Website serves the exact manifest/PNGs, and refuses a missing referenced frame. | Four native-valid fixture frames traverse real Website auth/ticket/proxy code. Live native manifest structure and sampled frame byte/RGB hashes also verified. |
| Offloaded evidence | The Website consumer preserves registry/object identity when backing bytes are unavailable and when they return. It does not fabricate missing objects or bypass access. | Controlled durable-origin case recovers an offloaded object through the real Website routes; corruption still fails. |
| Firebase tenant contract | Standard `firebase.tenant` is supported alongside consistent legacy aliases; conflicting claims fail closed for team access. The email mutation uses fresh Firebase-account authorization. | Installed Firebase Admin token type inspected; same-team/cross-team/conflicting-claim route regressions. |
| Notification source confusion | Current Website receipts join only the matching owner/run/result/digests. Pipeline's immutable notification snapshot is separately labeled. Notification-store failure does not hide the scientific result. | Source-binding, owner/public-redaction, outage, and rendering tests. |
| Failed email recovery | Explicit owner/operations email-only retry, immutable request ID, expected result digest, original recipient, total attempt cap three, transaction claim, preserved receipts, and no blind resend after ambiguity. | Fake sender tests cover authorization, forged recipient, same-key replay, concurrency, cap, digest drift, ambiguous outcome, and ownership change. Browser lost-response flow reuses one request key and simulates one send. |
| Notification polling versus submission | Read polling can observe delivery state but never invokes the retry action. Observation is bounded; actual retry requires an explicit POST authorization. | Hook, client, and sender tests; route is behind existing API CSRF and Firebase authentication. |
| Graphify unavailable | Runner accepts an existing isolated interpreter. The approved corpus now includes the result consumer and its delivery helpers. | Successful AST graph generation using the already-installed Pipeline virtual environment; zero model tokens; no installation. |
| Repeatable browser gate | Dedicated Playwright/Vite configuration serves the real client with mocked APIs, dev-only fake auth, a restricted environment, one worker, and loopback-only port. | `npm run test:result-consumer:browser`; no backend or production dotenv file is loaded. |

## Current live evidence, kept separate from the local candidate

A read-only refresh of the already authenticated result surface opened `capture-run-c257ae6e11a18e883637739477e5ded8`, matching the retained Scene 839873 run in the earlier audit.

- The Website record is `completed_unqualified`, with 20 delivered episodes, controls pending, and verified score/interpretation sidecars. The source contract validates after removing only the Website DTO's display-only visibility field.
- Replaying the current local comparison over that readback gives 10 paired cells, overall successes 1/10 and 6/10, observed paired magnitude 50 percentage points, and exact p=0.0625. No winner or readiness claim follows.
- The 3,389,649-byte full evidence manifest and 500,139-byte native frame manifest matched their published SHA-256 values. The full inventory has 5,056 descriptors after including its own manifest descriptor.
- All structural seals in the sampled native manifest validated. Three sampled frames matched both PNG-byte and decoded RGB digests, within a 6 MiB sample budget. This is explicitly not a full audit of every historical frame.
- A live 4 MB review clip initially returned a 403 ticket refusal. After page/session refresh, a decoded frame and 15-second duration were visible in the real browser. The local cross-tab CSRF regression addresses the independently reproduced token-rotation defect; no authentication bypass was used.
- The live progress page reports results ready and notification **failed**, while retaining a blocked execution-phase label. The local candidate separates these facts and supplies explicit, bounded email recovery.
- No matching delivery email was verified in the existing owner inbox. The connected organizational mailbox reported mail service unavailable. No new email was sent to manufacture proof.

Raw live records and manifests remain local evidence, not committed fixtures or public artifacts. Exported summaries omit storage paths, ticket signatures, recipient credentials, and provider tokens.

## Verification and operational boundary

Focused checks cover all original requested files plus the new integrity, full-inventory, source-binding, CSRF, query recovery, and notification-retry seams. Every Vitest/browser run uses one worker. TypeScript checking passes. Final logs, commands, source identity, screenshots, patch, and graph metadata are included in the local review bundle.

The historical inbox failure is not changed on the deployed Website: deployment and live sends were explicitly excluded. The local implementation and fake-sender recovery are complete; a future authorized owner can integrate the candidate and deliberately use its retry action. Likewise, no full production archive was downloaded and no policy result was requalified. Those boundaries are not silently replaced with fixture proof.

Integration must preserve the original local repair and the follow-up commit together. Do not cherry-pick a test-count receipt as if it were a deployment, a provider-zero receipt, or a scientific qualification.
