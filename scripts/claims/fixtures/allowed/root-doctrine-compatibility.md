The persisted `world_model` enum is an internal compatibility lane for Policy Improvement Run requests.
World models are generated/advisory policy-improvement support artifacts inside scoped runs, not the primary public offer.
