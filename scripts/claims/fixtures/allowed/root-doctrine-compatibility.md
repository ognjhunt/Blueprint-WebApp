The persisted `world_model` enum is an internal compatibility lane for Post-Training Data Package requests.
World models are generated/advisory data-package support artifacts inside packages, not the primary public offer.
